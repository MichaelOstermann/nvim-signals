return {
    batch = require("signals.batch"),
    batch_wrap = require("signals.batch_wrap"),
    computed = require("signals.computed"),
    effect = require("signals.effect"),
    signal = require("signals.signal"),
    untracked = require("signals.untracked"),
}
